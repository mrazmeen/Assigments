server:
  port: 8080
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8505/eureka
  instance:
    hostname: localhost
spring:
  application:
    name: allocationservice